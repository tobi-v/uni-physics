# Setup

Always make sure you are in the same directory as this README.md (\<repo\>/python/).

(First time setup see below)

## Windows with git bash
```bash
venv/Scripts/activate.bat
```

## First time

### Windows with git bash
```bash
python -m venv venv
venv/Scripts/activate.bat
pip install -r requirements.txt
python -m pip install -e .
```